{
  "name": "spiget",
  "version": "0.0.2",
  "description": "Javascript/Typescript client for the spiget.org API",
  "main": "index.js",
  "scripts": {
    "test": "mocha",
    "compile": "tsc",
    "generate-types": "node ./scripts/generate-types.js",
    "compile-generator": "rm -rf dist/generator && tsc -p tsconfig.generator.json",
    "build": "rm -rf dist && npm run compile"
  },
  "author": "Haylee Schäfer",
  "license": "MIT",
  "dependencies": {
    "axios": "^0.19.2"
  },
  "devDependencies": {
    "@types/node": "^14.0.14",
    "chai": "4.2.0",
    "mocha": "7.1.1"
  }
}
